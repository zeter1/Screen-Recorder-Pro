# 09. Чек-лист перед выдачей новой версии

## Код и структура

- [ ] Изменены только необходимые файлы.
- [ ] Нет случайного возврата к монолитному файлу.
- [ ] Совместимый `Screen Recorder Pro.py` сохранён.
- [ ] Не добавлен `__init__` в mixin без интеграции.
- [ ] Нет циклических импортов.
- [ ] Нет `@staticmethod`, обращающихся к `self`/`cls`.
- [ ] Runtime-папки не попали в архив/коммит без необходимости.

## Автоматические проверки

```powershell
python verify_project.py
python -m compileall .
python -c "from screen_recorder.app import ScreenRecorderProWin11; from screen_recorder.shared import APP_BUILD; print(APP_BUILD)"
```

- [ ] Все команды завершились успешно.
- [ ] Новая регрессия имеет автоматическую проверку, если это возможно.

## Видео

- [ ] `DXCAM_CAPTURE_ENABLED = False`.
- [ ] Для ddagrab используется wall-clock rebase.
- [ ] Ровно один `fps=<target>:round=near`.
- [ ] Нет `setpts=N*ticks` как основного таймлайна.
- [ ] Для ddagrab+NVENC нет `hwdownload`.
- [ ] Сохранён `-fps_mode passthrough`.
- [ ] 144 Гц / 72 FPS даёт poll 144.
- [ ] GDI и CPU fallback всё ещё строят валидные команды.

## Аудио

- [ ] Микрофон не выбирается как системный звук.
- [ ] Stereo Mix/loopback распознаётся.
- [ ] CoreAudio WAV корректно закрывается.
- [ ] Ошибка обязательного системного звука не маскируется успехом.
- [ ] Микрофон mono → stereo.
- [ ] A/V start/end проверяются после смешивания.
- [ ] CoreAudio WAV выровнен по первому реальному видеокадру через trim/delay.
- [ ] В файле 15 указано, был ли recovery-путь реально задействован.

## Потоки и процессы

- [ ] Нет долгой операции в GUI callback.
- [ ] Фоновые потоки не трогают Tkinter напрямую.
- [ ] Все новые subprocess управляются централизованно.
- [ ] Есть timeout и cleanup.
- [ ] Полный выход не обходит `exit_app()`.

## Логи

- [ ] Ошибки содержат traceback.
- [ ] Команда FFmpeg сохраняется.
- [ ] Создаются ожидаемые файлы 00–15.
- [ ] Bursty/stepwise прокрутка без технической корреляции не объявляется подтверждённым рывком.
- [ ] Логи ограничены по размеру.
- [ ] `APP_BUILD` соответствует версии.
- [ ] `DIAGNOSTIC_SCHEMA` обновлена при изменении структуры данных.

## Ручной Windows smoke test

- [ ] Программа запускается из распакованной папки.
- [ ] Трей и плавающая панель работают.
- [ ] Настройки открываются.
- [ ] Запись 30–60 секунд сохраняется.
- [ ] Прокрутка выглядит плавно.
- [ ] Пауза/resume работают.
- [ ] Микрофон и системный звук работают.
- [ ] Длительность реальная.
- [ ] Конец звука не обрезан.
- [ ] В логах нет подтверждённых timing errors.
- [ ] После выхода не осталось `ffmpeg.exe`.

## Выдача пользователю

В сообщении указать:

- что исправлено;
- какие файлы изменены;
- что проверено автоматически;
- что реально проверено на Windows;
- что осталось непроверенным;
- короткий сценарий проверки.

Нельзя писать «ошибка исправлена окончательно» или «запись точно плавная», если реальная запись на целевом Windows-компьютере не выполнялась.
