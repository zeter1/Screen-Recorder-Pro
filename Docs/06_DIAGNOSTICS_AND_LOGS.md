# 06. Диагностика и логи проблем

## Назначение

Логи должны позволить определить причину без догадок и без повторного воспроизведения бага.

Каждая запись создаёт отдельную папку в `Логи проблем`, если функция включена.

## Файлы сессии 00–15

| Файл | Назначение |
|---|---|
| `00_прочитать_нейросети_сначала.txt` | Краткая итоговая сводка и приоритет чтения. |
| `01_события_записи.jsonl` | Структурированные события этапов записи. |
| `02_ffmpeg_команды_и_вывод.txt` | Точные команды FFmpeg/ffprobe и важный stderr/stdout. |
| `03_подробный_лог_записи.txt` | Человекочитаемый последовательный лог. |
| `04_ошибки_и_трейсы.txt` | Ошибки, предупреждения и traceback. |
| `05_настройки_и_окружение.json` | Настройки, устройства, runtime, монитор, зависимости. |
| `06_отчет_плавности_для_ChatGPT_5.6_SOL.json` | Сводный технический отчёт плавности. |
| `07_ffmpeg_progress_по_времени.jsonl` | `frame`, `fps`, `speed`, `dup`, `drop`, `out_time` по времени. |
| `08_нагрузка_системы_по_времени.jsonl` | CPU, RAM, процессы, GPU/NVENC, swap, диск. |
| `09_анализ_одинаковых_кадров.json` | Визуальные различия и повторяющиеся изображения. |
| `10_автоматические_кандидаты_рывков.json` | Кандидаты для ручной проверки, не доказанные ошибки. |
| `11_версия_исходников.json` | Компактный manifest: список `.py`, размеры и SHA-256 конкретной версии. |
| `11_исходный_код_только_при_ошибке.py` | Ограниченный snapshot исходников; создаётся только если в сессии возникла реальная ошибка. |
| `12_готовый_промпт_для_ChatGPT_5.6_SOL.txt` | Инструкция для анализа папки логов. |
| `13_сводка_времени_и_дрейфа.json` | Сравнение медиатаймлайна с реальными монотонными часами. |
| `14_детальный_тайминг_видео.json` | Полный timing summary/PTS/DTS; в 01 и diagnostic сохраняется только короткая ссылка/сводка. |
| `15_синхронизация_системного_звука.json` | Якоря CoreAudio/первого видеокадра, применённый trim/delay, границы дорожек и фактическая проверка recovery-пути. |

## Граница session-log

`recording_problem_log_session_closed` является явной последней границей live-событий записи.
После неё скриншоты, настройки и запуск следующей записи не должны попадать в старый `01`-файл.
Фоновый post-save анализ использует замороженные пути своей сессии; старый worker не может закрыть
лог новой записи благодаря проверке owner session id.

## Глобальные диагностические логи

В корне `Логи проблем` могут находиться:

```text
diagnostic_<timestamp>_pid<pid>.txt
diagnostic_latest.txt
```

Они нужны для проблем запуска, интерфейса, настроек и процессов до создания папки записи.

## Потеря Desktop Duplication

Для перехода Windows на защищённый экран сопоставляй события:

1. `ddagrab_access_lost_detected` — stderr содержит точный DXGI-маркер, указан PID и generation;
2. `ddagrab_recovery_waiting_for_default_desktop` — входной desktop ещё не `Default`, stop-frame лишь ожидает финализации;
3. `ddagrab_secure_desktop_recovery_requested` — обычный desktop вернулся, записана длительность недоступности;
4. `automatic_segment_restart_succeeded` — новый сегмент реально запущен;
5. `capture_recovery_attempt_counter_reset` — новый сегмент стабильно отдавал кадры три секунды.
6. `capture_recovery_segment_validated` — конечная копия проверена по длительности
   и содержимому последнего кадра; поля `plan`, `tail_mean_absolute_error` и
   `audio_policy` описывают применённое восстановление. События запроса/перезапуска
   сами по себе не доказывают успешное заполнение промежутка.

Поля `capture_unavailable_seconds`, `input_desktop`, `process_generation` и
`stop_frame_padding` отделяют ожидаемое ограничение защищённого экрана от перегрузки
GPU/CPU или произвольного зависания. Полный stderr по-прежнему остаётся в
`03_подробный_лог_записи.txt`; чтение через PIPE не должно терять его хвост.

## Диагностика горячей клавиши и скриншота

Для проверки скриншота сопоставляй события по порядку:

1. `native_screenshot_hotkey_ready` — Windows подтвердил `RegisterHotKey`, записан `thread_id`;
2. `hotkey_registration_success` — выбран backend и зафиксировано состояние потока;
3. `hotkey_callback_received` — Windows действительно передал нажатие;
4. `screenshot_requested` — действие дошло до GUI-потока;
5. `screenshot_snapshot_started` — начато сохранение экрана до потери фокуса;
6. `screenshot_snapshot_ready` — сохранены размер кадра, время и масштаб координат;
7. `capture_region_selector_opened` — полноэкранное окно показывает сохранённый кадр;
8. `screenshot_annotation_tool_selected` — выбран собственный инструмент скриншота и видимость палитры;
9. `screenshot_annotation_color_selected` — цвет изменён без закрытия активной палитры;
10. `screenshot_annotation_size_selected` — изменена толщина карандаша или размер стрелки;
11. `screenshot_annotation_toolbar_moved` — сохранена новая глобальная позиция панели;
12. `screenshot_annotation_added`, `screenshot_annotation_undone` или
   `screenshot_annotations_cleared` — изменение пометок с их количеством;
13. `capture_region_selection_started` — мышь нажата в режиме `Область`;
14. `capture_region_selection_finished` — записаны статус, координаты, размеры, время, цвета и сводка пометок;
15. `screenshot_copied_to_clipboard` — пометки нанесены, область вырезана и помещена в буфер.

Статус `too_small` означает область меньше 16×16 пикселей, `escape` — явную отмену,
`selector_error` — исключение окна выбора. `screenshot_request_ignored` объясняет повторное
нажатие во время уже выполняющегося снимка. Поэтому отсутствие файла или события больше
не нужно трактовать по догадке.

Для `Print Screen` на Windows нормальный `screenshot_backend` — `windows_register_hotkey`.
Событие `screenshot_hotkey_backup_ready` подтверждает установку дополнительного
несупрессивного `keyboard`-обработчика. Он нужен, если другая программа записи экрана
поглощает клавишу до доставки `WM_HOTKEY`. Если сработали оба контура,
`hotkey_callback_duplicate_ignored` подтверждает, что второй снимок не создавался.
`native_screenshot_hotkey_failed` содержит код и текст ошибки WinAPI, например конфликт с
другой программой. `native_screenshot_hotkey_stop_result.thread_alive_after_join=false`
подтверждает, что при восстановлении после автозапуска прежний поток действительно закрыт.
Значение `keyboard_fallback` означает, что WinAPI-регистрация не удалась и временно используется
старый backend. Если нет `hotkey_callback_received`, сначала проверь события backend, а не захват экрана.

`screenshot_snapshot_failed` содержит исключение подготовки кадра. Поля
`size_matches_virtual_screen` и `coordinate_scale` позволяют отличить ошибку DPI/геометрии
от закрытия временной панели. Для исправного нового контура у
`capture_region_selector_opened.background_mode` должно быть значение
`frozen_snapshot_before_selector_focus`, а у итогового события — такой же источник кадра.
Для новой панели ожидаются `annotation_tools_enabled=true`,
`annotation_backend=screenshot_canvas_v3` и
`color_palette_behavior=persistent_below_active_draw_or_arrow_tool`. Поля `annotation_count`
`annotation_colors` и `annotation_sizes` итогового события показывают число пометок, реально
использованные цвета и размеры. `toolbar_position_source=settings` подтверждает восстановление
позиции после запуска. Сами длинные списки точек в лог не записываются.

Для плавающей панели записи событие `floating_panel_geometry_initialized` содержит
сохранённую и фактическую позицию, размер и рабочую область монитора без панели задач.
`position_clamped=true` означает, что устаревшие координаты автоматически возвращены
в кликабельную часть экрана. Короткий клик по индикатору фиксируется событием
`floating_panel_clicked`.

Общий `diagnostic_log` вызывается также из потоков трея, горячей клавиши и preflight.
В этих потоках настройки логов читаются только из обычного словаря, без обращения
к `Tkinter Variable`; это предотвращает взаимную блокировку GUI и файлового lock.

## Как читать логи рывков

Рекомендуемый порядок:

1. `00` — итог и версия сборки;
2. `04` — исключения и технические предупреждения;
3. `06` — агрегированная сводка;
4. `13` — реальная длительность и drift;
5. `14` — детальный timing/PTS/DTS, если короткой сводки недостаточно;
6. `02` — точная команда FFmpeg;
7. `07` — progress/stall/dup/drop;
8. `08` — нагрузка;
9. `09` и `10` — содержимое кадров;
10. `15` — системный звук и A/V-якорь, если использовался Python CoreAudio;
11. `11_версия_исходников.json` — точный отпечаток версии; snapshot кода читать только если он появился из-за ошибки.

## Классификация visual cadence

Low-cadence окно больше не считается предупреждением только по проценту изменившихся кадров.
Анализатор сначала классифицирует движение:

- `continuous_motion` — движение действительно непрерывное;
- `bursty_or_interrupted_motion` — прокрутка или анимация шла отдельными порциями;
- `stepwise_motion_source_fps_unknown` — источник обновлялся ступенчато и мог иметь собственный FPS;
- `mostly_static` — сцена в основном неподвижна.

Статус записи понижается только для непрерывного low-cadence движения, совпавшего с
`drop`, progress stall, timing/drift, активным swap I/O или перегрузкой CPU/GPU/NVENC.
Без технического совпадения это наблюдение и кандидат на ручной просмотр, а не доказанный рывок записи.

Высокая занятость RAM без фактического swap I/O также является наблюдением, а не
автоматически доказанной причиной рывков.

Поле `diagnostic_coverage.visual_motion_test` показывает качество самого теста. Если status равен
`insufficient_continuous_motion`, статичный ролик не подтверждает визуальную плавность даже при
идеальных PTS и `dup/drop=0`; нужен отдельный тест 30–60 секунд с непрерывным движением.

Для CPU первый warmup-sample исключается из статистики. `system_cpu_pressure` отличает единичный
пик от устойчивой перегрузки. После пяти последовательных samples `>=95%` не чаще раза в 20 секунд
и не более трёх раз за запись Windows PDH сохраняет `high_cpu_process_attribution`: восемь наиболее
активных процессов с именем instance, PID, CPU, RSS и status. Command line, пути пользователя и
содержимое процессов не читаются. Полный psutil-обход всех процессов не выполняется, потому что он
оказался слишком тяжёлым для записи. В сводке также доступны per-core пики и скорости disk I/O.

## Уровни причин рывков

Разделяй минимум четыре уровня:

1. **Захват** — Desktop Duplication не получил новое состояние экрана.
2. **Кодирование** — NVENC/CPU не успевал принимать кадры.
3. **Таймлайн/контейнер** — PTS/DTS, CFR, gaps, drift.
4. **Воспроизведение** — плеер, декодер, VSync, частота монитора.

## Что является сильным доказательством

- `Non-monotonic DTS`;
- обратные или одинаковые PTS в движении;
- много нулевых/очень коротких интервалов;
- серьёзные packet gaps;
- `speed` стабильно ниже реального времени;
- остановки роста `out_time`;
- рост `drop`;
- длительная перегрузка CPU/GPU/NVENC;
- активный swap I/O в момент проблемы;
- drift, растущий с длительностью;
- совпадение визуального кандидата с техническим событием.

Для живой записи отдельным сильным сигналом считается остановка роста `frame` при
продолжающем работать FFmpeg. События `recording_video_frames_stalled`,
`automatic_segment_restart_started`, `automatic_segment_restart_succeeded` и
`automatic_segment_restart_failed` показывают весь путь восстановления. Событие
`coreaudio_segment_rollover_requested` объясняет техническую смену сегмента на длинной
записи. Несогласованность RIFF-заголовка попадает в `wav_validation`.

## Что не является доказательством само по себе

- высокий процент одинаковых кадров на статичном рабочем столе;
- анимация приложения с 30 FPS внутри файла 72 FPS;
- один длинный первый интервал;
- `dup/drop=0` при визуальных повторах;
- ровные PTS;
- субъективное ощущение без контроля в другом плеере;
- единичный пик CPU без ухудшения progress.

## Правила записи ошибок

При исключении сохраняй:

```text
контекст операции
тип исключения
сообщение
полный traceback
ключевые пути
команду subprocess
return code
последний значимый stderr
состояние записи
```

Не перехватывай исключение пустым `except Exception: pass`, если оно влияет на пользовательский результат. Допустимый best-effort код должен оставлять диагностическое событие хотя бы уровня WARN.

## Ограничение размера

Логи имеют лимиты и автоочистку. При добавлении нового потока данных:

- используй JSONL для последовательных samples;
- ограничивай частоту адаптивно: подробно на старте/аномалии, реже в штатной работе;
- ограничивай размер файла;
- не дублируй одинаковые строки;
- сохраняй агрегированную статистику до удаления подробностей;
- при достижении лимита потокового лога сохраняй начало и свежий хвост, а не теряй конец записи.

Обычные и ошибочные логи по умолчанию хранятся 120 дней. Автоматическая и ручная очистка
старых логов обрабатывает только распознанные папки сессий и файлы `diagnostic_*.txt`,
созданные Screen Recorder Pro. Посторонние файлы и папки внутри `Логи проблем` пропускаются
и перечисляются в `cleanup_problem_logs_finish` как `skipped_unrecognized_names`.
Кнопка явного удаления всех логов остаётся отдельным подтверждаемым пользователем действием.

## Пост-анализ после сохранения

Тяжёлый анализ содержимого кадров (`09`/`10`) не должен задерживать готовый файл и UI.
Сначала выполняются остановка, сборка, `ffprobe`/тайминг и проверка результата. После этого
пользователь получает сохранённое видео, а визуальный анализ идёт в фоновом потоке.

Если начинается новая запись или программа закрывается, фоновый визуальный анализ отменяется,
чтобы его FFmpeg не конкурировал с основной записью за GPU, диск и декодер.

## Известная тонкость

Первичный placeholder в `06_...json` создаётся до завершения записи. Источником истины после сохранения являются финальный отчёт, точная FFmpeg-команда и `13_...json`. Не делай вывод только по полю placeholder, если оно ещё имеет статус `recording_in_progress`.
